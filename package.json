{
  "name": "soundboard",
  "version": "UNVERSIONED",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "css": "npx tailwindcss -i styles.css -o ./build/output.css",
    "css-watch": "npx tailwindcss -i styles.css -o ./build/output.css --watch",
    "build": "npx esbuild src/index.mts --bundle --outfile=build/index.js --platform=browser",
    "build-watch": "npx esbuild src/index.mts --bundle --outfile=build/index.js --platform=browser --watch",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jamalam360/soundboard.git"
  },
  "author": "Jamalam",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Jamalam360/soundboard/issues"
  },
  "homepage": "https://github.com/Jamalam360/soundboard#readme",
  "devDependencies": {
    "husky": "^8.0.0",
    "tailwindcss": "^3.3.5",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "crunker": "^2.3.0"
  }
}
